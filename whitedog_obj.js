function animal(obj){
    let color = obj.color;
    let name = obj.name;
    let legs = obj.legs;
    
    return `This ${color} ${name} has ${legs} legs.`
}

// var animal = obj => `This ${obj.color} ${obj.name} has ${obj.legs} legs.`;