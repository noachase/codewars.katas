function squaresNeeded(grains){
     
    let j = 1;
    let times = grains == 0 ? 0 : 1 
    
    
    // for (let i = 1; i <= grains; i++){
    // //   square++;
    //   if (j <= grains) {
    //      j*=2;
    //      ++times; 
    //   } else {
    //       return console.log(times);
    //   }
    // }

    while (j < grains) {
        
    }
}

squaresNeeded(1)